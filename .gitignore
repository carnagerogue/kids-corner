node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.tsbuildinfo

# Private logins — keep these off GitHub and out of the public build
src/data/credentials.local.ts

# Skill installs (npx skills add) — not part of the app
.agents/
.claude/skills/
skills-lock.json
kids-corner-guardian-*.zip
# ...except the load-unpacked package, which must be committed so hosting serves it
!public/guardian/kids-corner-guardian-unpacked.zip

# Kids Corner Guardian self-host signing key — NEVER commit
extension/guardian-signing-key.pem
