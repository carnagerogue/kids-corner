// Firebase web config for cross-device message sync.
//
// These are PUBLIC client values (safe to commit) — security comes from the
// Realtime Database rules plus the secret "family sync code" each device
// enters. Fill these in from your Firebase project's web-app config to turn on
// cross-device messaging; leave them blank to keep messaging local-only.
export const FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  databaseURL: "",
  projectId: "",
  appId: "",
};
