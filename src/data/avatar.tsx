// ---------------------------------------------------------------------------
// Coin economy constants. (The legacy 2D avatar — a hand-built SVG character
// with its own cosmetic catalog — was retired in favor of the 3D avatar; only
// these shared wallet constants remain. See src/features/avatar/ for the 3D
// system and the mystery box.)
// ---------------------------------------------------------------------------

/** Coins every learner starts with. */
export const STARTER_COINS = 60;

/** Coin cost of an extra mystery-box spin (the first each day is free). */
export const SPIN_COST = 40;
